# dht11-daq-arduino
A simple Data Acquisition (DAQ) project built with an Arduino Uno and a DHT11 sensor. Reads ambient temperature (°C) and relative humidity (%) every 2 seconds and logs the data as CSV output via USB serial to a connected computer.
